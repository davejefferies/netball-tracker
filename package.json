{
    "name": "Netball Tracker",
    "private": true,
    "scripts": {
        "prisma:generate": "\"npm run prisma:generate --prefix backend\"",
        "dev": "concurrently \"npm run start --prefix backend\" \"npm run web --prefix frontend\"",
        "dev:backend": "npm run start --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
